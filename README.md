## Run locally
1. Install packages:
    ```
    pip install -r requirements.txt
    ```

2. Install playwright's dependencies:
    ```
    playwright install
    ```

3. Start script:
    ```
    python jszp.py
    ```
